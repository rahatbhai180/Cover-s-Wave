// Order Button Alert
function orderNow(product, price) {
    alert(`আপনি "${product}" অর্ডার করতে চাইছেন।\nমূল্য: ${price} Tk\n\nঅর্ডার করতে Inbox/WhatsApp/Nagad number দিন!`);
}

// AOS init + GLightbox
AOS.init();
const lightbox = GLightbox({ selector: '.glightbox' });

// Auto slide images for each product slot
const slots = document.querySelectorAll('.product-slot');
slots.forEach(slot => {
    const images = slot.querySelectorAll('.product-slider a img');
    let index = 0;
    images.forEach(img => img.style.opacity = 0);
    images[index].style.opacity = 1;

    setInterval(() => {
        images[index].style.opacity = 0;
        index = (index + 1) % images.length;
        images[index].style.opacity = 1;
    }, 3000);
});